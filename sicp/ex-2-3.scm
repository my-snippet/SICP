(load "ex-2-2.scm")


;; If it takes two segments as the arguments to create a rectangle,
;; it will be more complex ( Direction, start and end points, etc. ).
;; Therefore here it takes 4 points as the arguments
