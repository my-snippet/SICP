(load "lib/3/stream/stream.scm")

;; Just call the representative library
;; For example, stream.scm is the representative, 
;; it calls other stream libraries


;; And all works should be located in [PROJECT ROOT]/sicp/
